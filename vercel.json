{
    "version": 2,
    "public": true,
    "installCommand": "npm cache clean --force && npm install",
    "build": {
        "src": "./index.js",
        "use": "@vercel/node"
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}